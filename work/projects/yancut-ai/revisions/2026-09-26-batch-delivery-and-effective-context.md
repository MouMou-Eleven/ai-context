# 完整迁移批次与有效沉淀

日期：2026-09-26。建委要求尽快、最大程度还原Vercel已有能力，不删减赶工；尽量一次交付可一起上传的代码包，并将经验有效沉淀到百度秒哒板块。

## 原因与新旧差异

R8曾先交执行说明、被再次要求后才交代码包；测试依赖Windows临时绝对路径，云端需二次适配；上下文已到R8，但源码远端main为80d49a91、codex/miaoda-runtime为b74e3395（本日ls-remote核对），R8另在本地交付目录。不能推断代码丢失或Skill天然低效。

新方式：R8/R9/R10作为内部范围，必要多包同批上传后顺序应用；总验收为清单和测试，不另造重复覆盖总包。未知差异只阻塞冲突目标，其他独立开发继续；失败问题保留原编号。测试须使用包内相对路径和目标实际文件，Codex负责源码对齐。

## 当前事实

用户提供R8-RESULT.md及v28截图，报告00018迁移、AI模块、Edge部署、44条Mock断言和v4 lockfile修复；真实模型至MP4未测。用户发布app-enipq7iozwn5.miaoda.online，浏览器独立看到首页和登录页，广场显示请求来源不允许；尚未取得云端有效Origin配置，不宣布根因就是域名漏配。

R8实物迁移名为00018_r8_ai_attempt.sql；本地r8-source另有00018_r8_ai_plan.sql。下一批按实际包核对，不把同序号当同文件，不重跑未知旧迁移。

## 本次迁移范围

1. 对照原站能力与R8实际包，建立可重建基线，保护v24认证热修和云端配置。
2. 收口来源问题、lockfile和测试入口；迁移ASR报价、异步任务、音频归属/时长、幂等扣返、字幕与口播衔接。
3. 声音、广场、自动成片、平台状态和包装按原接口逐项对照；原站本缺Key或外部Worker的边界保留，禁止占位凑数。
4. 能力矩阵、前端保留、双用户权限、短片导出与回滚验收。包数在代码与体积核验后确定，目标一次1–5包、每包低于20MB（用户当前入口反馈）。

## 有效沉淀

完整方法在[百度秒哒增量正文](../../../domains/development/tools/miaoda/experience/patterns/codex-miaoda-iterative-increment-workflow.md#一次交付批次与有效完成)，README/llms与自然任务路由提供入口。用自然说法验正向触发，以飞书妙搭、套餐查询验不误触发。路由通过不等于功能完成，实际代码包仍须解压应用复测。

## 本轮已落地与验证

- 直接修正百度秒哒增量方法正文中的串行交付误解，而非仅新增心得；保留云端冲突写入串行和依赖失败停止的要求。
- 更新板块README、llms、经验入口与任务路由；“秒哒尽快还原原站”“秒嗒后面的包一次给齐”“秒哒批量交付减少上传轮次”“Vercel迁移到miaoda.online”均能找到该方法，飞书妙搭、会员容量、纯状态查询不误加载。
- 项目README将v28回执、独立浏览器所见、未验收能力分开；R6.1旧状态移入历史，并补齐R7/R8修订索引和中文用途。
- 全仓1274文件校验0错误；122项回归通过。两条警告为既存上游快照重复文件保留，不是本轮失效链接。
- 上一轮只完成交付方案及上下文修正，当时代码包尚未生成；建委随后明确要求直接生成，实际交付结果见下方B1记录。

## 下次执行入口

无需让建委重传已应用的R8原包。以v28实际部署为起点，只纳入有证据的R8必要修补及后续实现；若一个ZIP容纳完整批次即交一个，体积超过当前入口限制才分为最多五个有序包。不要再提供内容重叠、覆盖关系不明的独立“总包”。

验收清单须逐项列：原站证据、迁移目标、自动测试、云端实测、未完成依赖。账号/积分/存储、规划与执行、ASR字幕、声音任务、广场发布播放撤下、自动成片、包装与导出均需对照；原站尚未完成的能力单列后续迭代，不能混称迁移损失或已恢复。先实现测试再交付，包内携带实际代码、SQL、安装校验、回滚及统一执行说明，用户只需一次上传与一次综合回执。

## B1实物交付（建委要求直接生成后）

- 已生成一个代码ZIP：`YanCut-RESTORE-B1-20260926.zip`，共794个文件、1,894,526字节；SHA256：`482657032ad00d5f655606b531f811d0e674a94eb51772797fa3d1c234d294fa`。
- 本机交付位置：`C:/Users/Administrator/Documents/Codex/2026-09-26/ch/outputs/YanCut-RESTORE-B1-20260926.zip`。包内START-HERE为统一执行入口，无需再次上传旧R8。
- 实现：ASR真实WAV时长验证/报价/幂等提交/恢复/字幕导入；声音预留与结算/音频私有转存/实扣暂停；广场发布播放撤下；自动成片分镜与包装队列；购买兼容、签名回调、后台渲染配置、定时处理及延迟文件清理。新增SQL仅00019→00020→00021。
- R8规划器仅定点接通服务能力判断；保留原协议和账本。保护认证/工作台热修；精确增加当前生产来源，修复video标签不经过fetch桥的签名播放地址及长请求超时。
- 验证：111项新增契约断言＋44项R8回归；真实SQL/PGlite、双函数路由/CORS/权限、安装预检/幂等/冲突/逐字节回滚、Deno两个入口检查通过。最终ZIP在另一目录独立npm ci、整包复测及源码重建后payload hash一致。
- 实物复测确实发现构建注释包含不同依赖安装目录，导致重建hash漂移；已修正构建输出并重新打包、解压、复测。此后“可重建”应以新目录实测及hash为证据，不能只测试原开发目录。
- 包含原源码59个方法/路径合同对照；这不是59项生产验收通过。仍未调用真实付费供应商、未在本轮部署秒哒、未完成用户素材MP4整链。外部Worker、平台定时调度、密钥与历史数据迁移按清单单列。
- 本次并未宣称私有源码迁移分支已完整回收云端全部热修；后续须以本包源文件/manifest及秒哒实际回执继续对齐，不能直接用旧b74e3395覆盖部署树。
