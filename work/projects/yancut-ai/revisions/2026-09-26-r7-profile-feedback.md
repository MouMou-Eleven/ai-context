# R7 用户反馈、低成本增量与账户补充

## 当前执行卡

- 用户确认已上传上一包；密码提示、找回和修改密码目前正常。未确认整个注册/双用户/对象存储/积分链路通过。
- 新问题：登录/注册提示区空白太大；登录后先显示匿名再更新；要求头像、昵称编辑，隐藏内部映射邮箱。
- 秒哒截图声称迁移/构建通过，并记录旧 yancut-api 500、billing/support 404；只作为排查线索，不当作标准。继续保留现行 v2 路由、短信桥、Deno/依赖/构建配置。
- 用户否定每轮付费全量导出：使用已有完整源码＋实际已交付增量叠加，单文件哈希冲突时定向取证。

## 根因与上一轮纠错

`min-h-20`固定反馈区造成空白。实际R7 ZIP没有studio-shell账户确认态、server/auth.ts/platform.ts或本地类型声明；之前本地开发树和实际交付物不一致，不能用前者测试覆盖后者。上一轮源码审查中的“独立验证”只对当时开发树成立，不能继续作为包完整性证明。

当前包 `yancut-JW-20260926-R7-PROFILE-v1.zip`，SHA-256 `af86affff38dd392b6cc251704dfe7192fd18f77ad2bab99a870f6ee3770b6e8`，31469 bytes，10个变更文件。原导出hash `8b28ca3dab464b2714cb24092b72b98e180decfbcd61f31ef317b356b5fd42f7`＋上一包hash `98e3b50a5bc33ceb2c687fdc77528676cb26f0bbfce7712a9c439caa94ddd32b`为重建基线。安装器逐项验证before/target哈希，备份后应用；不含密钥、运行配置或历史恢复SQL。

## 实现和边界

- 无消息时不占固定80px反馈区；仍保留真实错误反馈。
- 登录请求后等待服务端账户快照再导航，头像/积分一起显示，不假造余额；网络慢时按钮忙，不宣称零等待。
- 账户页编辑头像/昵称，头像本机裁成128px PNG小图写既有users.image，服务端64KiB及尺寸校验；姓名和头像保存后同步页面并支持重载。隐藏内部邮箱但不修改登录身份。
- `00017_r7_profile.sql`为唯一新迁移，service-only RPC，不改认证/积分/管理员。未登录、额外userId/isAdmin字段和非法头像均拒绝。
- R8 AI/编辑、R9 ASR字幕、R10广场声音、总验收仍是后续计划，不能用本轮账户功能通过宣布R7整体完成。

## 验证分层

真实PGlite执行迁移及Edge→SQL保存、用户隔离/权限/非法字段共13断言通过。完整前端tsc与Vite3024模块构建通过。浏览器在1440和390宽度检查间距≤40px、无横向溢出；模拟300ms账户读取下登录导航无匿名按钮，昵称和头像保存/刷新/导航同步通过，无pageerror。前端为模拟传输，实际手机及秒哒上线验收仍由用户按独立清单验证。

包的10文件校验、全量导出＋上一包＋本包实物重建用于交付检查，不再以开发树替代。若秒哒报文件差异，仅回报路径/hash，按需回传单文件；不默认增加用户全量导出积分支出。
