# R7 源码回收与增量包审查（2026-09-26）

## 事实

- 用户回传秒哒完整前后端导出和 `R7-BACKEND-BASELINE.zip`。导出源码不包含历史 `tasks/` 构建目录，不能把它当作无需补充即可复建的完整构建上下文。
- 实际 Edge 源码已经实现项目保存、重开、revision 冲突和媒体元数据接口；旧的 `R7-PENDING-APIS.md` 将这些路径重复列为未迁，后续以实际 `server/projects.ts`、`server/media.ts` 为准。
- 原云端上传客户端仍调用 Vercel Blob。开启秒哒后端不代表对象存储链路已接通。

## 本轮交付

生成 `R7-INCREMENT-R6-R7-20260926.zip`，SHA-256 `98e3b50a5bc33ceb2c687fdc77528676cb26f0bbfce7712a9c439caa94ddd32b`，26 个 manifest 文件，另含安装说明、问题清单和证据回传要求。R6 认证收口与 R7 基础能力分在清单中分别验收。

- 认证：注册/找回 OTP purpose 分流、空 204 兼容、限流错误、密码即时不一致提示、会话读请求去重/超时/迟到响应保护、SPA 登录退出和跨标签会话失效。
- R7 基础：项目/媒体隔离回归、私有 `yancut-private` bucket 配置迁移、签名上传/读取、预留配额、完成时大小/MIME 核验、过期预留清理、引用保护删除、客服二维码 Edge 接口和运行时计费支持读取。
- 构建：补充无密钥 `tasks/build-static-preview.sh`，只读取公开 `VITE_*` 变量并在缺失时停止。

## 独立验证

- 前端 `tsc --noEmit`、Vite 生产构建（3023 modules）通过。
- Deno `check`（补足导出树缺失的本地类型引用后）通过。
- PGlite 真实 SQL：迁移幂等、项目重开/冲突/双用户隔离、媒体隔离、积分 charge/refund 幂等、文件归属/大小/上传窗口/引用删除保护通过。
- Playwright：密码不一致即时提示及清除、反馈区域几何不位移、6 种宽度无横向溢出、Logo、找回密码眼睛按钮通过；未发送真实短信。

## 尚未验收

云端实际上传和签名 URL、找回密码短信、真实账号登录退出、管理员二维码上传、正式域名版本切换仍须秒哒部署后手动验证。声音、ASR、AI 规划、画廊、Remotion/HyperFrames 属后续阶段，本包没有伪装成已完成。
