# B3：编辑器运行时、页面体验与真实动画任务

日期：2026-09-27。来源：用户指定对话 `01a0dd1d-ccb1-7c73-9cc3-4233827c67a7` 的0:53需求、7张截图、B2-RESULT、GLM上传文档，以及续接对话的完整需求。用户已明确B2上传并执行完成；旧B2修订中“待应用”是当时状态，保留历史，不能再要求重做B2。

## 当前交付与一致性

- B3本地交付，尚未应用/正式发布秒哒；未部署Vercel。
- `YanCut-B3-20260927.zip`：5,314,737字节；SHA256 `68d17b2bdac5f7c72ef993103cba600c0a435402559c64240c6a75328c9303c4`。
- 64目标文件、314唯一上下文补丁段。安装器整体预检、备份、幂等、冲突拒绝、回滚保护；不改.env、认证桥、不新增SQL。云端现有源码未完整回收，重叠冲突须定点合并并留diff。
- 交付目录：`C:/Users/Administrator/Documents/Codex/2026-09-27/codex-threads-01a0dd1d-ccb1-7c73-9cc3-2/outputs`。含ZIP、SHA256、CHECKLIST、VERIFICATION、START-HERE及秒哒执行提示词；ZIP内保留证据与B3-RESULT回执模板。
- 本地完整B2独立源码叠加后，64文件逐字节核对、TypeScript、Vite3029模块构建（27.42秒）、双Edge Deno check通过。依赖复用锁对应已有node_modules，不冒充新机完整依赖安装。
- ZIP独立解压，npm ci --include=dev后通过79安装断言、46 SQL/handler、18传输、44规划、120实际服务代码/SQL断言，以及Worker2组行为测试。网络供应商边界mock，无真实付费调用。

## 本次实现

页面：持久Studio路由、预热项目、广场缓存；未知项目数不假0，换号隔离索引、拒绝迟到响应、失败可重试。恢复价格中心套餐/积分包与联系客服文案，素材卡全宽。二维码读取改受控原始字节→Blob→解码，上传后重取并释放旧URL。

配置：GLM-5.3-Flash/FlashX下拉和自动协议参数，默认 `https://open.bigmodel.cn/api/paas/v4`，普通管理员只填Key。保留Seedance中转音频任务/轮询/usage.amount与currency合同；不能混用MiniMax官网协议。

编排：8个实际参与运行的场景模块，统一规划器与确定性执行器，28类命令合同。借鉴auto-motion的分镜与逐段验收思路，未直接嵌入仓库。各场景可独立维护，但还不是8套成熟专用Agent；AI仍未覆盖全部人工功能。

运行时：离线保留时间戳的960宽代理，代理预览画面/原片音频；导出保存暂停、WASM合成串行、复制画布、30秒帧等待/60秒步骤等待限制、取消/失败释放；解码缓存有界；预览与导出母带处理对齐。

动态动画：`render_motion`输入Remotion TSX或HyperFrames HTML/GSAP，自包含源码，经付费确认/UUID幂等任务，完成下载为本机素材，插入同一时间线且可撤销；工程版本变化拒绝过期插入。原片留本地。Worker使用单实例持久串行队列、32待处理、48h终态清理、重启恢复及签名输出。生产模型代码必须进无网/只读/非root/资源限额Docker容器。双Edge补generated源校验、幂等ID转发及丢响应找回。

## 实测证据

- Chromium界面模型配置、二维码上传/读取、价格导航和购买弹窗通过模拟API；真实编辑器插入/多选AI删除/撤销重做/鼠标拖动裁剪缩放通过。Windows WebKit界面通过。
- 6秒720p含特效、关键帧、中文、贴纸与AAC导出1.9001秒；取消/解码失败后缓存归零。
- 8秒4K30fps高码率片段54,758,731字节：12次定位原片平均425.225ms，960×540代理38.842ms；代理生成1.3375秒，原片转1080p导出3.2177秒。24来源后8缓存/~49.8MB帧估算，清理后0；不是浏览器总内存。
- 5种音频格式真实解码、增益/静音/偏移，预览同款母带与导出最大采样差7.45e-9。
- Remotion4.0.506及HyperFrames0.8.78各2秒720p可信fixture实际出片并检查抽帧；HyperFrames严格lint通过，复跑约7.5秒。直接执行的是自行编写可信fixture，不是生产模型代码。

## 待验收与下一步

1. 当前秒哒源码预检/定点合并、真实Auth/存储/二维码/充值，正式部署状态。
2. 真实GLM多模态与Seedance配音/实扣；本批未调用付费供应商。
3. 秒哒目标主机host-probe、Docker镜像构建/隔离、持久卷/重启、公网任务和扣费回填全链。开发机Docker daemon未运行，不能声称容器通过。秒哒不支持长任务时需独立HTTPS Worker，上传ZIP不会自动配置服务器。
4. macOS Safari、独立Edge/Firefox、长片/VFR/HDR、多路变速音频、数小时编辑与多用户并发；Windows WebKit不能代替Safari验收。
5. AI全部人工功能映射、每场景质量样例集。生成动画限自包含源码、30秒、不透明视频，不能逐层编辑。浏览器进程崩溃仅恢复已保存工程，无法断点续编码。

每项详见交付CHECKLIST；不得把短样例快、本地构建通过或HTTP成功当作商业化验收完成。后续先读取本修订，再读取云端B3-RESULT，避免重复要求用户解释前文。
