# R8实际AI接线包（v27梳理之后）

用户回传截图明确本轮143任务仅做勘察，未写代码；ai/*仍503。billing/support报告200是线索，不能继续沿用404已确诊，也不独立认证通过。用户要求实际增量包，继续R8，不进入R9。

交付yancut-R8-AI-IMPLEMENT-20260926.zip，180632字节，SHA256 eb21b92549b263ece19bb4c6f04f1f687c476e31386e82814be93c43e401722b。

内容：原规划器/技能/证据/四协议打包为server-only ESM，新增ai.ts两个接口；00018追加service-only原子占用+扣费/结果+退款RPC。相同用户/UUID/hash重放，不重复调用；不同内容拒绝；超过10分钟processing要求对账不自动重试。无模型不扣费且不伪造本地计划。语音/外部渲染保持不可用门禁。模型输入是浏览器当前工程上下文，不声称服务端验证本地原片归属。

安装器预检全部目标后备份，新增模块并定点插入handler；api-client仅将AI规划传输超时20秒改90秒，保留v24四文件热修。锚点冲突只取具体文件，不要求全量导出。保护运行配置、用户、管理员和账本，不重跑旧SQL。

本地：真实规划器+实际PGlite SQL+Edge共44断言通过，四协议传输Mock不收费。完整前端tsc及Vite3024模块通过；新增ai.ts Deno通过。完整Edge Deno检查因原导出缺tasks/integration/vendor-types/zod/index.d.ts及关联隐式any失败，须用秒哒现行配置复核，未宣称全通过。ZIP CRC/逐文件字节验证、解压应用及重复预检通过。

尚未真实供应商、浏览器执行导出闭环。异常Worker终止/数据库失败可能保留processing，已有明确错误与不重复收费策略，但无后台对账UI或自动恢复；正式收费上线前必须完善运维对账。R7未验收项继续保留。INSTALL与独立问题验收清单一起交付，要求秒哒在本轮处理平台适配并回传分层结果。
