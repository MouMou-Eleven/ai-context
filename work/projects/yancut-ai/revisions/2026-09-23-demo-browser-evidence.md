# 2026-09-23：录屏主线真实浏览器验收

## 目标

本轮录屏主线固定为“口播精剪＋字幕＋画面包装”。验收使用全新本地工程和真实可解码的 22.4 秒中文口播测试片，避免复用此前已经被多轮试验污染的工程。

## 已验证

- 新建干净工程并导入本地 MP4，时间线先保留完整素材。
- 本地规则规划识别“先剪停顿、保留原声、先不加包装”，只生成停顿检测步骤，没有被上下文中的能力说明误触发配音、包装或字幕。本轮浏览器测试不作为在线 GLM 规划质量的证据。
- 执行后真实检测并删除 7 处停顿，共减少约 8.6 秒，时间线从 22.4 秒变为约 13.9 秒；执行回执包含实际 revision 变化。
- 字幕面板默认中文；本地 Whisper 真实生成中文字幕轨道。合成测试语音的识别出现错词和简繁混用，必须校对；仍返回长句时间范围，不能作为逐字对齐或字幕成片质量通过的证据。
- 第二轮对话要求“片头标题：言剪 AI；信息条：对话式剪辑；保持字幕安全区和原声”。本地规划准确生成两步：文字安全区适配、可编辑动效包装。
- 执行后时间线出现“言剪 AI”和“对话式剪辑”两个文字元素，回执为 2/2 完成；包装以原生文字、标签和关键帧写入，不依赖独立云渲染服务。
- 选中标题后属性面板显示实际文字内容和“水平偏移”关键帧开关；点击后按钮进入 pressed 状态，证明手动关键帧入口可操作。
- Vercel Preview `https://yancut-ai-personal-oedzhlxbe-jianweiyang12131-3768.vercel.app`（`dpl_2vz8Z2Xx5ENgq3QGHQmdWa7AqmfY`，源码 `958ac2c8`）Linux 构建及 TypeScript 检查通过，部署状态 Ready；浏览器确认 `/studio` 页面可访问，首页显示个人测试边界和 AI 设置入口。固定个人测试域名 `https://yancut-ai-personal.vercel.app` 已指向此部署。
- 最终全量测试 344 pass、0 fail、3283 assertions，覆盖 63 个文件；TypeScript 检查通过。
- 本地生产构建退出码 0；Windows standalone 文件复制存在 `node:crypto` 文件名警告，因此不将该 standalone 包作为已验收交付物。部署采用 Vercel Linux 构建。
- 修复文字安全区仅保留前三行导致字幕丢字的问题。识别请求词级时间戳，并依据实际词边界组句；本次浏览器素材仍返回长句，因此新增明确校对提示，不虚构对齐精度。再次识别需确认新增轨可能重叠，不覆盖人工轨。

## 导出边界

首次自动化没有捕获下载事件，随后修复导出反馈：成功后保留“视频已生成”、文件大小和“下载成片”重试按钮，并延迟回收下载 URL。复测浏览器下载事件为 completed，1,614,960 字节；ffprobe 验证成片 14.001633 秒、H.264＋AAC、640×360。工程重开后素材、剪辑与包装均恢复。本次是小型合成测试片，不代表高清长片或所有浏览器均通过。

导出窗口关闭时销毁该次下载状态；再次打开重新显示格式/画质设置，避免编辑后仍下载旧成片。操作卡保存在源码仓库 `docs/yancut/demo-product-acceptance.md`。源码已推送至远端 `main` 的 `958ac2c8`。

## 未宣称能力

- 在线 Vercel 环境不包含用户素材，也不会替用户保存 GLM Key；需要用户在“AI 设置”中手动输入本人 Key。
- 用户已提供豆包服务开通信息；本轮尚未提交真实豆包 ASR 任务，标准版仍需可供供应商读取的 HTTPS 音频地址及小文件实际验收。
- 本地字幕测试是合成中文语音，真人口播识别质量、长视频流式处理、完整语义高光和云端渲染队列仍未完成系统验收。
- 开发服务器日志仍会出现 Radix hydration ID 差异和 Fast Refresh 提示；生产 Preview 的页面主流程可访问，提示不应混写成业务功能失败。
