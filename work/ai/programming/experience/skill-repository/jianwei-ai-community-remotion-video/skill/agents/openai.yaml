interface:
  display_name: "jianwei-ai-community-remotion-video"
  short_description: "把一句话或参考图导演成自然、有记忆点的参数化 Remotion 动效"
  default_prompt: "Use $jianwei-ai-community-remotion-video to treat an uploaded reference image as the geometry- and background-locked target for the entire animation. First present the visual-owner, sampled-background, layout, visibility, energy-chain, camera-response and fidelity plan for my approval. After approval, silently expand my simple request into a validated internal production brief without changing approved facts, then build a parameterized Remotion project with natural event-driven motion and no full-frame terminal reference overlay. Keep protected text and visual elements fully visible: measure real glyph/visible bounds after fonts load, use temporary padded reveal masks, release clipping before the hold, and stress-test the longest editable values. Render a complete low-resolution preview first, audit visibility, background regions and final-hold continuity, and wait for my explicit final-render approval; text, numbers, and colors must be editable from Studio's right-side Default Props inspector."
