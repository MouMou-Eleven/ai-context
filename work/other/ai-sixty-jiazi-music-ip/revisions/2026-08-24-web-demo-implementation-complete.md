# 2026-08-24：Web Demo 实现完成

## 背景

建委确认“批准、继续、授权”，并要求按已有理解继续强化设计。这一确认结束了初始修订中“待视觉批准后开发”的阻塞。

## 本次新口径

- Superdesign 设计从 v2 迭代为 v3“甲音 · 玄金夜谱精密古音仪”，作为已批准的实现基线。
- Web Demo 已不是三个工具的整合方案，而是可运行的五模块产品原型：听命、寻甲子、合音、造曲、神谱。
- 首版音频使用 Web Audio 合成，不直接打包 690 MB 原始 WAV；权属和压缩策略确认后再替换或混合。
- 角色风格从初版 `2.8 头身收藏潮玩` 强化为 `7–7.5 头身成熟东方神格概念`；先建立可信的神格责任与世界观，再在后续转译为手办比例。
- 当前不做 App Store 封装或小程序开发；继续以网页验证核心体验。

## 实现与验证

- 本地源码：`C:\Users\Administrator\Documents\居- 项目\ai-sixty-jiazi-demo`。
- 完成 60 条甲子数据机械导出、历法样例、环形最短音程修正、Web Audio 可听原型和非空 Melody 作曲简报。
- 通过 TypeScript 检查、6 项单元测试和 Vite 生产构建。
- 以 Playwright 在 1440 × 1000 与 390 × 844 进行桌面/移动验收，最终新会话 0 错误、0 警告。

## 工具偏离记录

- ImageGen 内置工具两次返回 404；用户已授权 CLI 备选，但当前进程、用户和机器环境均未配置 `OPENAI_API_KEY`，因此没有伪造角色图输出。
- 本轮以已批准的 Superdesign v3 和五组成熟神格生成提示词作为视觉交付；后续工具恢复后再生成并做一致性评审。

## 对后续工作的影响

- 后续迭代应以当前 Demo 和本修订记录为起点，不要回到旧工具并页或 v2 待批准状态。
- 正式公开前仍要完成历法边界、文化字段、四柱权重和音频版权复核。
- 待建委确认独立源码仓库名称、可见性和部署目标后，再推送完整代码并进行线上部署。
