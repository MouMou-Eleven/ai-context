# 2026-08-13 本地开发目录迁移

## 迁移结果

- 原目录：`C:\Users\Administrator\Documents\Codex\2026-08-10\w1\work\yancut`
- 当前目录：`F:\桌面文件\言剪AI`
- Git 分支、HEAD、索引、未提交改动、本地 `.env.local` 和关键源码哈希均比对一致。
- 可重建的 `node_modules` 与 `.next` 缓存未直接复制，在新目录按 `bun.lock` 重新安装和构建。
- 新目录生产构建通过，`http://127.0.0.1:3417/studio` 返回 HTTP 200。
- 原目录已移入 Windows 回收站，避免后续在两套源码中误改。

## 故障说明

迁移前 3417 端口被原目录的残留 Node 进程占用，但请求持续超时，导致本地链接无法打开。停止该进程并从 F 盘新目录启动生产服务后恢复。

## 后续规则

后续所有言剪 AI 本地开发、构建和启动操作默认使用 `F:\桌面文件\言剪AI`。如果再次迁移，必须同步更新项目 README 和本修订索引。
