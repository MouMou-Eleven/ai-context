# STRUCTURE.md — 仓库结构与写入规范

> **⚠️ 任何 AI 模型在向本仓库写入内容前，必须先阅读本文件。**
> 本文件定义了仓库的目录结构、文件命名、写入格式和渐进式披露规则。
> 即使你不了解本仓库的历史，只要遵守本规范，写入的内容就不会与现有结构冲突。

---

## 一、仓库定位

本仓库是建委的**个人 AI Context 档案**，核心用途：
- 让任何 AI 工具快速了解建委是谁、在做什么、偏好什么
- 统一管理所有项目资料，一个仓库搞定一切
- 采用**渐进式披露**结构，AI 按需读取，节省上下文

---

## 二、目录结构

```
ai-context/                          ← 仓库根目录
├── README.md                        ← 仓库入口，简要介绍 + 引导 AI 读 STRUCTURE.md
├── STRUCTURE.md                     ← 📌 你正在读的文件（仓库规范）
├── identity.md                      ← 个人身份信息（变化频率：低）
├── current.md                       ← 当前状态（变化频率：高）
├── preferences.md                   ← 沟通偏好和工作方式（变化频率：低）
│
├── knowledge/                       ← 知识与技能（按主题拆文件）
│   ├── achievements.md              ← 成就、奖项、荣誉
│   ├── interests.md                 ← 兴趣方向
│   ├── tech-stack.md                ← 技术栈
│   ├── tools.md                     ← 工具清单
│   └── ai-programming/              ← AI 编程类工具子目录（按工具拆子目录）
│       ├── README.md                ← AI 编程大类入口
│       └── miaoda/                  ← 百度秒哒（MIAODA）实战经验库
│
├── history/                         ← 历史记录
│   └── timeline.md                  ← 事件时间线（按时间倒序追加）
│
└── projects/                        ← 所有项目（每个项目一个目录）
    ├── README.md                    ← 项目索引（渐进式披露入口）
    ├── feishu-efficient-office/     ← 示例：书稿项目
    │   ├── README.md                ← 项目概述 + 文件索引 + 使用指南
    │   ├── writing-style-analysis.md
    │   └── all-docs.md
    ├── videoai/                     ← 示例：VideoAI 项目
    │   └── README.md
    └── openclaw-agent/              ← 示例：OpenClaw 项目
        └── README.md
```

### 结构规则

1. **根目录只放档案文件**：identity.md、current.md、preferences.md
2. **项目统一放 `projects/` 下**：每个项目一个独立目录
3. **项目目录名用 kebab-case**：全小写，单词间用连字符（如 `feishu-efficient-office`）
4. **项目目录内必须有 README.md**：作为该项目的入口（渐进式披露第一层）
5. **knowledge/ 按主题拆文件**：每个主题一个 .md，不要合并成一个大文件
6. **knowledge/ 下允许有大类子目录**：当某主题下沉淀的内容增多（多文件 + 多工具），可以建大类子目录（如 `ai-programming/`），子目录内按工具再分（如 `ai-programming/miaoda/`），子目录必须有 README.md 作为大类入口；新增 AI 编程类工具（如 Cursor、Claude Code、v0 等的实战经验）应放在 `ai-programming/<工具名>/` 下

---

## 三、渐进式披露规则

**核心原则**：AI 不应该一次性加载所有文件，而是按需读取。

### 读取顺序

```
第1步：README.md（仓库入口，~1KB）
  → 了解仓库是什么、有哪些目录
  
第2步：根据任务选择
  → 了解建委是谁？→ 读 identity.md
  → 了解当前状态？→ 读 current.md  
  → 了解某个项目？→ 读 projects/README.md（项目索引）
  → 了解写作风格？→ 读对应项目的 README.md 中的索引
  
第3步：深入
  → 按项目 README.md 的指引，读取具体文件
```

### 项目 README.md 必须包含

每个项目的 README.md 应包含以下部分：

```markdown
# 项目名称

> 一句话介绍（状态：进行中/已完成/暂停）

## 项目概述
简要说明项目的目标、背景、当前阶段。

## 文件索引
| 文件 | 内容 | 何时读取 |
|-----|------|---------|
| `xxx.md` | 内容说明 | 读取时机说明 |

## 使用指南
对于 AI：应该怎么用这些文件。
对于建委：内容怎么维护。
```

**关键**：文件索引表让 AI 知道**该读哪个文件、什么时候读**，避免盲目加载。

---

## 四、写入规范

> **⚠️ 写入任何文件前，必须先确认该文件是否已存在，不要覆盖已有内容。**

### 4.1 新增项目

1. 在 `projects/` 下创建新目录（kebab-case 命名）
2. 创建 `README.md`（必须包含：概述、文件索引、使用指南）
3. 更新 `projects/README.md`（在项目列表中添加新条目）
4. 按需创建其他文件

### 4.2 更新现有项目

1. 先读取该项目的 `README.md` 了解结构
2. 按照现有结构追加或修改，**不要改变目录结构**
3. 如果需要新增文件，更新项目 README.md 的文件索引表

### 4.3 更新个人档案

| 文件 | 更新频率 | 写入规则 |
|-----|---------|---------|
| `identity.md` | 低 | 仅补充，不重写 |
| `current.md` | 高 | 替换为新内容 |
| `preferences.md` | 低 | 仅补充，不重写 |
| `knowledge/*.md` | 中 | 追加新内容到对应文件 |
| `history/timeline.md` | 中 | 在文件顶部追加新条目（最新在前） |

### 4.4 文件格式规范

- **编码**：UTF-8
- **换行**：LF（Unix 风格）
- **语言**：中文为主，技术术语可用英文
- **标题层级**：`#` 用于文件标题（每个文件只有一个），`##` 用于章节，`###` 用于子章节，最多到 `####`
- **列表**：用 `-` 无序列表，用 `1.` 有序列表
- **表格**：用标准 Markdown 表格
- **引用**：用 `>` 引用块
- **强调**：用 `**粗体**` 强调关键词，不要过度使用

### 4.5 禁止事项

| ❌ 禁止 | 原因 |
|--------|------|
| 创建与现有结构冲突的新目录/文件 | 破坏渐进式披露 |
| 删除或重命名现有文件（除非明确要求） | 破坏其他 AI 的引用 |
| 在根目录创建项目文件 | 根目录只放档案文件 |
| 使用 camelCase 或 PascalCase 目录名 | 统一用 kebab-case |
| 覆盖已有文件而不确认 | 可能丢失重要内容 |
| 创建空目录或只有注释的占位文件 | 浪费空间，增加噪音 |

---

*本规范随仓库演进持续更新。最后更新：2026年5月22日。*
