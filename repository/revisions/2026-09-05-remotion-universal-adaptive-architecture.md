# Remotion Skill 重构：通用自适应架构

日期：2026-09-05

本次不是针对单个缺陷追加补丁，而是把 Skill 重构为统一的自适应流水线：输入归一化、结构识别、导演决策、空间选择、稳定布局、帧驱动运动、保真控制和模式化验收。旧版 `motion-director` 作为创意基线吸收，Remotion 作为唯一实现基座。

统一底层不变量覆盖所有输入：事实不臆造、参考图目标帧、唯一核心视觉、动作因果、稳定阅读区、固定几何锚点、完整可见、确定性渲染、参数真实绑定和证据化验收。具体案例只提供测试样本，不再写入 Skill 的核心逻辑。
