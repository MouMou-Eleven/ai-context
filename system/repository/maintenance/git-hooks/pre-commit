#!/bin/sh
# Validate the staged snapshot. Deliberately never run git add.
repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 1
exec sh "$repo_root/system/repository/maintenance/git-hooks/run-python" "$repo_root/system/repository/maintenance/pre-commit.py"
