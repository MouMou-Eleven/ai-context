#!/bin/sh
# Self-heal the desktop viewer when the system/repository is reopened or switched.
repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
sh "$repo_root/system/repository/maintenance/git-hooks/run-python" "$repo_root/system/repository/maintenance/desktop-sync.py" || true
